{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type":"shell",
            "command": "cmake --build build",
            "group": "build"
        },
        {
            "label":"Build and run",
            "type":"shell",
            "command": "cmake --build build && ./build/app"
        }
    ]
}